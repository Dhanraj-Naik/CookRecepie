import React from 'react';
import { Text, View } from 'react-native';

const Details = ({
    navigation,
}) => {
    return (
        <View>
            <Text>Recepie Details</Text>
        </View>
    );
}

export default Details;
